# 日志文件压缩备份工具 LOG-RAR 0.3

------

```
0.3, 2017-01-04, same.
  新增：按年和月分子目录。
  修正：处理月和日加0变双位数时会依赖系统短日期格式的问题。
  修正：未指定rar路径导致指定工作目录时找不到rar的问题。
0.2, 2016-12-25, same.
  新增：支持对单一不分日期的日志文件每天压缩存储多个版本，目前为存储最多100个。
  新增：支持对单一不分日期的日志文件改名后立即初始化，尽量避免丢失日志的风险，一般情况下也可以免掉 log-init.lst 了。
0.1, 2016-12-24, same.
  新增：支持初始化日志文件清单 log-init.lst
0.0, 2016-12-23, same.
  初始版本。
```

```
log-rar [path] [sep]
	path	待压缩备份日志文件所在路径，默认为当前目录。
	sep	待压缩备份日志文件名中的日期分隔符，默认为减号（-）。

例如：
log-rar C:\AppServer

	就会压缩 C:\AppServer\log-rar.lst 里列出的大小超过8192字节的文件到
	C:\AppServer\log-bak\[year]\[month] 子目录里，并删除原文件。大小未超过8192字节的，
	则直接移动到 C:\AppServer\log-bak\[year]\[month] 子目录里。
	如找不到文件，则加上当前年份前缀（如：2016-），再试一次。

log-rar.lst 例子（每日按 yyyy-mm-dd.log 文件名格式保存的日志，已省略年份）：
	01-01.log
	01-02.log
	... ...
	12-31.log
```
